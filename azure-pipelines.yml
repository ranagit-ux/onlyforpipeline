# Starter pipeline
# Start with a minimal pipeline that you can customize to build and deploy your code.
# Add steps that build, run tests, deploy, and more:
# https://aka.ms/yaml
name: infrapipeline
trigger:
- main
pool: autopool
stages:
  - stage: 
    jobs:
      - job: terraformintallation
        steps:
          - task: TerraformInstaller@1
            inputs:
              terraformVersion: 'latest'
      - job: Terraforminit
        dependsOn: terraformintallation
        steps:
          - task: TerraformTaskV4@4
            inputs:
              provider: 'azurerm'
              command: 'init'
              workingDirectory: '$(System.DefaultWorkingDirectory)/ranagit-ux/onlyforpipeline'
              backendServiceArm: 'Free Trial(b7d2b67b-62bb-4340-a048-868c00c1a9be)'
              backendAzureRmResourceGroupName: 'forpipeline'
              backendAzureRmStorageAccountName: 'mypipelinestorage'
              backendAzureRmContainerName: 'pipelinecontainer'
              backendAzureRmKey: 'terraform.tfstate'